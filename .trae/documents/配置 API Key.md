# 配置 DeepSeek API Key

1.  **检查环境文件**: 检查 `.env` 是否存在。
2.  **配置密钥**: 将用户提供的 API Key `sk-c2e6c9e14777460596941f4f2a5806bc` 写入 `.env` 文件。
3.  **安全检查**: 确认 `.gitignore` 中包含了 `.env`，防止意外泄露。

配置完成后，CodeSoul 就能真正连接到 DeepSeek 的“灵魂”了。
